- page_children.children.live.each do |children_page|
  %ul
    %li
      = link_to children_page.title, refinery.url_for(children_page.url)
      - if children_page.children.any?
        = render 'sitemap/page', locals: {:page_children => children_page}
